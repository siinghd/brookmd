# Required by the androidTest AndroidX dependencies (androidx.test:runner etc.);
# this module is a standalone Gradle build, so the flag can't come from a root
# project. Jetifier is NOT needed (no legacy support-library deps).
android.useAndroidX=true
