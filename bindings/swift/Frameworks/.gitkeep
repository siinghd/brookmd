# Placeholder so the Frameworks/ dir exists in git.
# scripts/build-xcframework.sh assembles brook_md_ffi.xcframework here (macOS + Xcode);
# the XCFramework itself is a native binary artifact and is never committed.
